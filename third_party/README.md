# third_party

This directory is reserved for small vendored dependencies if they become necessary. Prefer system
packages or CMake `find_package` dependencies for OpenCV, Eigen, Ceres, GTSAM, Pangolin, and Open3D.

